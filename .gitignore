# ---------------------------------------------------------------- secrets
# Nila makes no API calls and ships no keys. These patterns are here so that
# stays true: anything anyone adds later lands in a file git will not take.
.env
.env.*
!.env.example
*.pem
*.p12
*.jks
*.keystore
client_secret*.json
service-account*.json
secrets.properties
local.properties

# ------------------------------------------------------------ build output
build/
.gradle/
.kotlin/
*.apk
*.aab
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/

# ------------------------------------------------------------------ python
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# -------------------------------------------------------------- ML working
# Datasets are downloaded by ml/src/download.py -- 2.8 GB of ESC-50,
# UrbanSound8K and Donate-a-Cry that belong to their publishers, not here.
ml/data/

# Training artefacts are regenerated by the pipeline. The copies the app
# actually ships live in android/app/src/main/assets and are tracked.
ml/models/
ml/cache/

# Large models are downloaded on demand at runtime, never committed.
*.task
*.litertlm
*.pkl
*.keras

# ...with one exception. The optional language models are .task files fetched at
# runtime into /sdcard, which is what the rule above is for. The pose landmarker
# is a .task file too, and it is the opposite kind of thing: 9 MB that ships
# inside the APK so the safety watch works offline from first launch, exactly
# like the .tflite and .onnx assets beside it. Ignoring it silently produced an
# APK whose camera lane fell back to face presence with no indication why.
!android/app/src/main/assets/*.task

# --------------------------------------------------------------------- misc
.DS_Store
Thumbs.db
*.log.gz
